{
  "private": true,
  "main": "handler.js",
  "description": "Fetches the last SHiFT code tweet from https://twitter.com/borderlands",
  "scripts": {
    "deploy": "serverless deploy --aws-profile private",
    "start": "dotenv serverless offline"
  },
  "dependencies": {
    "node-fetch": "^2.3.0"
  },
  "devDependencies": {
    "dotenv-cli": "^2.0.0",
    "serverless-offline": "^4.9.4"
  }
}
