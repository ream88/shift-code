{
  "private": true,
  "description": "Fetches the last SHiFT code tweet from https://twitter.com/borderlands",
  "main": "handler.js",
  "scripts": {
    "deploy": "dotenv -- serverless deploy --aws-profile private",
    "start": "dotenv serverless offline",
    "test": "node test.js"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "dotenv-cli": "^3.2.0",
    "nock": "^13.0.4",
    "serverless-offline": "^6.5.0"
  }
}
